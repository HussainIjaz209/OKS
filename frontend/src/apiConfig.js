// API Base URL configuration
// For production (Vercel), this will use the environment variable
// For local development, it defaults to localhost:5001
export const API_BASE_URL = import.meta.env.VITE_API_BASE_URL || 'https://oks-backend.vercel.app';
